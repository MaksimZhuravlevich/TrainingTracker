# Copy to ../.env (project root) or webapp/.env and fill in values.

SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=TrainingTrack_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Production (DEBUG=False): set CSRF_TRUSTED_ORIGINS to your domain(s), comma-separated
# CSRF_TRUSTED_ORIGINS=https://example.com
